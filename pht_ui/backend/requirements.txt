# UI-layer dependencies — install ON TOP of paper_annotation/requirements.txt
# (which already brings in pymupdf, sentence-transformers, faiss-cpu, openai,
#  google-genai, huggingface_hub, pydantic, python-dotenv, etc.)
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
